//! noer-std: biblioteca padrão inicial da linguagem Noer.
//! Nesta fase, os módulos são placeholders para evolução nas fases 5+.

pub mod crypto {
    /// Placeholder de API de cifra para a fase inicial.
    pub fn encrypt_stub(input: &[u8]) -> Vec<u8> {
        input.to_vec()
    }
}
